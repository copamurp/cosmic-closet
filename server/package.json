{
  "name": "cosmic-closet-server",
  "version": "1.0.0",
  "description": "Cosmic Closet server",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.mjs",
    "dev": "nodemon server.mjs",
    "build": "npm install && cd../client && npm install && npm run build && cd../server && npm install && npm run start"
  },
  "keywords": [],
  "author": "Connor Murphy",
  "license": "ISC",
  "homepage": "https://github.com/copamurp/cosmic-closet#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.1.3",
    "express": "^4.18.2",
    "mongodb": "^5.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
